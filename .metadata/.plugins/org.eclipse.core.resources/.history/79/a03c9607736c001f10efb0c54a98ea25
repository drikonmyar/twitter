body {
	font-family: Arial, sans-serif;
	background-color: #f4f4f4;
	margin: 0;
	padding: 0;
}

.container {
	max-width: 600px;
	margin: 50px auto;
	padding: 20px;
	background-color: white;
	box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.home-container {
	margin: 0;
	padding: 0;
	width: 100%;
	height: 100%;
	display: flex;
	justify-content: center;
	align-items: center;
}

h2 {
	text-align: center;
}

form {
	display: flex;
	flex-direction: column;
}

label {
	margin: 10px 0 5px;
}

input[type="text"],
input[type="email"],
input[type="password"],
textarea {
	padding: 10px;
	font-size: 16px;
	border: 1px solid #ccc;
	border-radius: 4px;
	margin-bottom: 20px;
}

input[type="submit"] {
	padding: 10px;
	font-size: 16px;
	border: none;
	border-radius: 4px;
	background-color: #1da1f2;
	color: white;
	cursor: pointer;
}

input[type="submit"]:hover {
	background-color: #0d8dec;
}

textarea {
	resize: vertical;
	height: 100px;
}

#tweets ul {
	list-style-type: none;
	padding: 0;
}

#tweets li {
	padding: 10px;
	border-bottom: 1px solid #ccc;
	margin-bottom: 10px;
}

p {
	text-align: center;
}

/* Ensure there is enough space for the heart icon on the right */
.tweet-list li {
	position: relative;
	/* For absolute positioning of the heart icon */
	padding-right: 70px;
	/* Padding space for the heart icon */
	padding-bottom: 10px;
	/* Add padding at the bottom */
	border-bottom: 1px solid #e0e0e0;
}

/* Style for the heart icon */
.like-button {
	position: absolute;
	right: 10px;
	/* Position the heart button on the right */
	top: 50%;
	/* Align vertically in the middle of the tweet */
	transform: translateY(-50%);
	/* Center the icon vertically */
	color: #999;
	cursor: pointer;
	font-size: 18px;
	display: flex;
	align-items: center;
}

/* Optional hover effect */
.like-button i {
	margin-right: 5px;
	transition: color 0.3s ease;
}

.like-button:hover i {
	color: #e74c3c;
	/* Red color on hover */
}

/* Adjust size and color for the like count */
.like-button span {
	font-size: 14px;
	color: #333;
}